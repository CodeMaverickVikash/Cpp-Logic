#include<bits/stdc++.h>
using namespace std;

// Pairs : Pair is used to combine together two values which may be different in type
int main() {

	// pair<int, int> p = {1, 3};
	// cout<<p.first<<" "<< p.second;

	// pair<char, int> p;
	// p = make_pair('c', 12);
	// cout<<p.first<<" "<< p.second;


	// pair<int, pair<int, int>> p1 = {1, {3, 4}};
	// cout << p1.first << " " << p1.second.first << " " << p1.second.second;

	// Array of pair
	// pair<int, int> arr[] = { {1, 2}, {2, 5}, {5, 1} };

	// // Prints 5
	// cout << arr[1].second;

	return 0;
}